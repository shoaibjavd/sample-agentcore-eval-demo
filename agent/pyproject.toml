[project]
name = "assistant-agent"
version = "0.1.0"
requires-python = ">=3.12"
# Versions pinned exactly — see mcp-server/pyproject.toml for rationale.
dependencies = [
    "strands-agents[otel]==1.28.0",
    "bedrock-agentcore==1.18.1",
    "strands-agents-tools==0.2.21",
    "aws-opentelemetry-distro==0.19.0",
    "httpx==0.28.1",
    "boto3==1.43.73",
]
