[project]
name = "assistant-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "strands-agents",
    "bedrock-agentcore",
    "strands-agents-tools",
    "aws-opentelemetry-distro",
    "httpx>=0.27.0",
    "boto3>=1.34.0",
]
